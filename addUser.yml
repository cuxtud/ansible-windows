---

- name: Add a user
  #hosts: "{{ morhost }}"
  hosts: all
  gather_facts: false
  tasks:
    - name: Add User
      win_user:
        name: aditya
        password: "{{ lookup('cypher','secret=secret/aapassword') }}"
        state: present
